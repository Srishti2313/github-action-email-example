name: Email Step Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f2002313@ds.study.iitm.ac.in
        run: echo "Hello, world!"
